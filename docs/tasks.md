# 작업 목록

지금 열려 있는 요청과 그 상태. 끝난 항목은 커밋 해시와 함께 아래 "완료"로
내린다. 새 요청은 받은 순서대로 "대기"의 끝에 붙인다.

## 진행 중

- 없음. 받은 요청은 모두 처리했고, 아래 "완료"에 커밋과 함께 적어뒀다.

## 대기

1. **Swift 6 언어 모드로 올리기** — 진행 중. `SWIFT_STRICT_CONCURRENCY: complete`
 기준 경고가 292개(Puck 220 + PuckClient 72)에서 **46개**(두 타깃 합쳐 고유 기준)로 줄었다.
 지금까지 한 것: AppDelegate와 상태 기계(StateHandler·CharacterController)와
 NSViewRepresentable 코디네이터들을 `@MainActor`로, 큐로 지키는 타입들을
 `@unchecked Sendable`로, 순수 헬퍼는 `nonisolated`로. 그 과정에서 point_at이
 타임아웃 시 백그라운드 큐에서 캐릭터를 만지던 진짜 버그가 드러나 고쳤다.
 메뉴바·오버레이 컨트롤러까지 `@MainActor`. 남은 큰 덩어리는 진짜로 동시적인
 쪽이다: `CodeEditorRunner`(14), `LoopbackHTTPServer`(12), `AgentHost`(11),
 `AcpAgentProcess`, `BridgeConnection`, 툴 핸들러 몇 개. 0이 되면 `SWIFT_VERSION`을 6으로 올린다.
2. **계속 리뷰하고 고치기** — 새로 짠 코드를 같은 방식으로 훑는다.

## 완료

- 전체 리뷰 한 바퀴 + 동시성 정리 (`HEAD`) — 리뷰가 찾은 5건(디스플레이 변경 시
  섬 좌표/스케일이 남는 문제, 잡고 있음 플래그가 걸리는 문제, 오르는 중에
  포커스 창 설정이 발을 놓게 하던 것, 주석 부패)을 고치고, 알림 관찰자를
  스스로 등록 해제하는 `NotificationTokens`로 모아 메인 액터 타입들의 deinit
  문제를 없앴다. `get_frontmost_window`가 다른 스레드에서 갱신되는 창 목록을
  읽던 진짜 경합도 그 과정에서 드러나 고쳤다.
- 어항에서 잡으면 펫이 탈출하던 것 (`HEAD`) — 커서가 펫 위에 오면 오버레이가
  클릭을 받는데, 보통 창은 클릭하면 그 앱이 앞으로 나온다. 그래서 펫을 잡는
  순간 채팅 창이 프론트에서 밀렸고, 그게 바로 펫을 데스크톱으로 보내는 조건이었다.
  오버레이를 non-activating 패널로 바꿔 앱을 활성화하지 않게 했고, 잡고 있는
  동안에는 집/데스크톱 결정 자체를 멈춘다(놓으면 그 사이 보고된 것으로 다시 결정).
- 어항에서 더 활발하게 (`HEAD`) — 섬에서는 배회 간격 8~15초 대신 3~7초, 한 번에
  2~4개 구간을 걷고 사이 멈춤도 0.2~0.7초. 섬에서는 오를 창도 장난감도 없으니
  "가만히 있기"만 따로 줄였다.
- 허공을 타고 올라가던 것 (`HEAD`) — 벽을 다시 확인하는 쪽(`windowBeingClimbed`)에는
  "그 모서리가 실제로 보이는가" 검사가 없었다. 화면 가장자리를 공유하는 창이
  넷이면 x=0에 벽이 넷 쌓여 있고 그중 앞의 하나만 그려지는데, 뒤의 것도 벽으로
  쳐서 아무것도 없는 선을 타고 올라갔다. 이제 오르기를 시작할 때와 같은 규칙
  (가려지지 않은 모서리 + 오르지 않기로 한 창 제외)을 매 프레임 적용한다.
- 해상도가 바뀌면 펫이 화면 밖으로 떨어져 못 돌아오던 것 (`HEAD`) — 창은 다시
  만들어지는데 펫이 걷던 영역은 옛 화면 크기 그대로라, 바닥이 새 화면 아래쪽에
  생겨 거기로 떨어졌다. 이제 다시 만들 때 영역을 다시 재고 펫을 그 안으로
  들여놓는다(제자리 유지, 순간이동 아님).
- 아바타 다시 불러오기 버튼 (`HEAD`) — 폴더에서 스프라이트나 manifest를 고친 뒤
  앱을 껐다 켤 필요 없이 지금 펫을 다시 만든다.
- 어항 그림을 Canvas 한 번으로 (`HEAD`) — 복사본마다 뷰를 두고 매 프레임 큰 PNG를
  다시 줄이던 것을 한 번 해석해서 직접 그리도록. 이음매는 반 포인트씩 겹쳐서
  경계선이 보이지 않게.
- 섬을 스플릿 위로 올려 한 뷰로 다시 그리고, 손잡이를 눌러 접을 수 있게
  (`f2fc1d8`). 접으면 설 자리가 없다고 알려서 펫이 데스크톱으로 돌아간다.
- 기본 대화 이름을 "새로운 대화"로 통일하고, 기본 세션도 첫 메시지로 제목이
  바뀌게 (`HEAD`)
- 워크스페이스 우클릭 삭제 (`HEAD`) — pet-app이 레지스트리를 들고 있으니
  창은 요청하고, 실제로 지워졌다는 확인이 와야 사이드바에서 사라진다.
- 어항이 파일 탐색기 위까지 이어진다 — 두 열이 각자 자기 조각을 그리고, 펫에게는
  합쳐서 알려준다(하나로 그릴 수가 없다, 스플릿이라).
- 마이크가 실제로 켜졌는지를 창이 안다 (`HEAD`) — 음성 인식 권한이 없으면
  누른 press는 시스템 프롬프트에 쓰이고 아무것도 녹음하지 않는데, 버튼만 켜져
  있었다. 이제 pet-app이 실제 상태를 답하고, 권한이 없으면 펫이 그 사실을 말한다.
  (로그를 보니 이 기계의 speechRecognition 권한이 실제로 꺼져 있었다.)
- 대화 찾기 필드를 캡슐 모양으로
- 사이드바 설정 버튼 제거, 툴바 "에디터 떼기" 버튼 제거, 코드 열을 접으면
  터미널도 함께 접히고, 탭 스트립의 터미널 버튼 제거(툴바에 있다) (`HEAD`)
- 갈아끼우는 것들을 한 폴더로, README에 가이드 (`HEAD`) —
 `~/Library/Application Support/Puck/{Avatars,Tank}`. 수조 그림도
 `Tank/seabed.png`로 덮어쓸 수 있고, 설정에 그 폴더를 여는 버튼을 뒀다.
- 수조 배경은 그림 하나로 고정, 이어붙일 때 반전 없음 (`HEAD`) — 고르는 메뉴와
 그라데이션 일곱 개를 걷어냈다.
- 리뷰·리팩토링 (`8f49228`, `be54420`, `HEAD~1`) — 커진 두 파일을 나누고,
 그 과정에서 사라져 있던 사이드바 검색창을 되살리고, `run_shell`이 64KiB~128KiB
 출력의 가운데를 조용히 버리던 것을 고쳤다(그 테스트는 고치기 전 코드에서 실패).
- 사이드바 개편 2차 (`e542140`, `a6db314`, `58c8cf0`) — 워크스페이스로 이름 통일,
 기본 워크스페이스는 목록에서 빼고 그 대화를 "채팅 및 작업"으로, 워크스페이스를
 누르면 펼쳐져 그 안의 대화가 보이게, "새 워크스페이스" 자리에 전체 목록 보기,
 행 호버(AppKit 트래킹 영역 -- `.onHover`는 List 안에서 안 불린다), 선택·호버
 하이라이트를 한 모양으로.
- 어항에 바다 그림 + 유리 (`7b828c2`, `d92b739`) — 높이에 맞춰 넣고 좌우는 잘리게,
 넓으면 좌우 반전해서 이어 붙인다. 섬 밖 배경은 투명(같은 파란색을 깔았더니
 섬 경계가 사라져 "박스 탈출"로 보였다).
- 탐색기 네비게이터화 (`1b8bd43`) — 필터를 트리 아래로, 변경된 파일만 보기,
 선택이 에디터를 따라가기.
- 참고 그림 기준 UI 개편 1차 (`6ec0a09`…`db22980`) — 입력창을 한 상자로 다시
 (모드 · 사고량 한 덩어리, +, 마이크, 위쪽 구분선 제거), 마이크가 실제로
 Puck의 푸시투토크를 잡도록 소켓 메시지 신설, 사이드바를 "시작 / 프로젝트 /
 채팅 및 작업" 세 묶음으로, 탐색기 전환기를 툴바의 빈 밴드로, 대화 글자 16pt와
 컬럼 760, 다른 세션이 도는지 스피너로 표시.
- UI 전반 크기 한 단계 올리기 (`HEAD`) — 타이포 토큰을 macOS 기본 스케일에서
 한 칸 올려 고정 크기로 못박고, 그에 맞춰 행 높이들을 키웠다. 남은 건 눈으로
 보고 다시 재는 것.
- 입력창을 한 상자로 (`8374ee0`) — 메시지 위, 아래 왼쪽에 첨부(+), 아래 오른쪽에
 모델·사고량, 실행 중에는 중지. 화살표 전송 버튼은 뺐고 Return이 보낸다.
 음성 버튼은 넣지 않았다: 음성 인식은 Puck 쪽에만 있고 PuckClient에서 누를 수
 있는 게 아직 없다. 소켓 너머로 눌러줄 메시지를 새로 만들어야 한다.
- 파일 에디터: 탭 사이 이동(⌘⇧[ / ⌘⇧]), 줄로 이동(⌘L), 파일에서 찾기(⌘F),
 이름으로 빠른 열기(⌘⇧O) (`cf2eeed`, `108ea2d`, `da82cf9`). 심볼 목록은 아직.
- 장난감이 구르고, 멈춘 걸 펫이 듣는다 (`01ea747`) — 차고 나면 옆속도가 그대로
 남은 채 한 프레임에 멈춰 서던 것, 던진 장난감이 발밑에 떨어져도 펫이 최대
 15초 동안 모르던 것.
- 두 앱의 테마·외관 맞추기 — PuckClient가 밝게/어둡게 설정을 아예 안 따르고 있었다
 (메뉴·팝오버·유리 배경이 시스템 것 그대로). 이제 그 값도 건너간다. 반대로
 PuckClient 설정에서 테마를 바꾸면 Puck 설정 패널이 옛 값을 계속 보여주던 것도
 같이 고쳤다.
- 섬 좌우 여백 20 → 10
- 외부 코드리뷰 8건 전부 (`a625f32`, `a6f00a3`, `1609f21`, `98208da`, `f32ad7e`,
`aafa38b`) — bridge.sock 인증(같은 사용자의 아무 프로세스나 툴을 실행시킬 수
있었다), 실행이 자기 세션을 들고 다니게, 음수 `Content-Length` 크래시,
`run_shell` 출력 상한, MCP 토큰이 비기 전에 발급, PID 대신 flock,
아바타 매니페스트 경로 가두기, 설치는 바꾸기 전에 다 확인
- 커밋 기록에서 Claude 흔적 제거 — 크레딧이 붙어 있던 144개 커밋을 다시 쓰고
강제 푸시했다. 남은 "Claude"는 코딩 CLI 제품명뿐이다.
- 빌드 도구 점검 (`d0b067f`, `40eca4d`) — 매번 프로젝트 재생성, 리소스 확인을
설치·테스트 양쪽에서, Metal 툴체인 확인, 우리 소스의 경고에서 테스트 실패
- 코드블럭에 테두리와 복사 버튼 (`fb84a8f`, `05906bc`)
- 집(고정) 버튼과 pin 기능 제거 (`a2dfa11`)
- 섬 어깨 값을 40 / 48로 확정하고 임시 조절기 제거 (`fb84a8f`)
- 답변 시 펫 머리 위 말풍선 제거, 탐색기 상단 여백 (`6f4ffbb`)
- 섬 어깨를 실시간으로 맞춰볼 수 있는 임시 조절기 (`88d84a7`)
- 창을 옮기면 어항도 따라간다 (`95f3362`)
- 리뷰·수정 반복 2차 (`e5a2b5e`…`6c7bc62`) — 파일 작업 실패가 조용히 묻히던 것,
재연결 후 펫이 못 돌아오던 것, 비행 중 크기 변경, 슬라이더 영역 제외가 실제로
적용되지 않던 것, 설정의 영어 절반, 설치 로그의 가짜 실패, 디스패치 id 재사용
- 리뷰·수정 반복 라운드 (`42947b6`…`3612861`) — git 경로 형식 고정, `.env` 모양
유지, 좁은 어항에서 펫 크기 맞추기, 닫힌 창의 유령 어항, MCP 마커 문자열
오탐, `deinit` 누락 둘, 저장 키 상수화, 새 테스트들
- `/permissions` · `/skills` 와 `/` 자동완성 (`6f16a3d`, `d2cc6a5`)
- 실행할 때 마이크를 묻지 않도록, 설치가 번들을 지우지 않도록 (`e835258`)
- 에디터 열림 상태 기억, 사이드바 브랜치 최신화, 트랜스크립트 헤드 UTF-8 절단
(`e292876`, `5fdcb0f`)
- ⌃`로 어디서든 터미널 열기, 파일 없이도 열리도록 (`8041438`)
- 이번 주 코드 자체 리뷰에서 나온 넷 (`c79506f`) — 셸 무한 재시작, 뱃지 비용,
git 중복 실행, 이름 변경 포커스
- 참고 UI 밀도: 파일 트리의 git 표시, 사이드바 · 푸터의 브랜치, 경로 breadcrumb,
탐색기 상단 여백 정리 (`4a935ec`)
- 프레임 안에서의 상태 전환 순서 규칙화, IUO 제거 (`ab28797`)
- 리뷰 지적 사항 대부분 (`5e2e1c2`, `935144e`, `8545afa`) — 데이터 레이스,
`deinit` 누락 셋, 접근성 권한 재시도, 소켓 디렉터리 권한, 송신 크기 상한,
인증 순서, 탭 포커스, 바운스 중복, 툴 결과 코딩 중복
- 창을 오갈 때의 반응 속도 (`80becc9`)
- 펫 크기 조절 바를 섬 왼쪽에 세로로 (`fc56022`)
- 탐색기에서 이름 변경 · 휴지통 · 새 파일/폴더 (`4c4f492`)
- 코드 에디터 아래 터미널 (`b101fa8`)
- 섬이 툴바의 빈 띠까지 올라가는 모양, 리퀴드는 다시 걷어냄 (`27f61cc`)
- 모든 떠 있는 패널을 같은 유리로 (`204e345`) — 위 커밋에서 되돌림
- 어항 세로 크기 조절 (`2f6912b`)
- 사이드바와 섬 색 통일 (`a36775c`)
- 창 배경 반투명화, 섬은 불투명 유지
- 어항 → 플로팅 리퀴드 아일랜드, 순간이동 대신 크기까지 함께 줄어드는 이동
- 펫이 뒤로 올라가거나 허공을 타던 문제
- 설정 창 테마 적용과 섹션 제목 크기
- 오른쪽 사이드바 탭 (파일 / 세션 기록 / git)
- 실시간 코드 반영과 에이전트 커서 따라가기
- 오토 액셉트, `/effort` `/model` `/fast`, 스킬 목록
- 코드 스타일 통합과 한국어 · 영어 전환

